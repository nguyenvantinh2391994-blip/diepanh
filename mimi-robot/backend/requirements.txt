# Mimi Robot Backend - Dependencies
# Chạy: pip install -r requirements.txt

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# AI - Ollama (chạy local, MIỄN PHÍ)
aiohttp>=3.9.0  # Để gọi Ollama API

# Speech - 100% OFFLINE
edge-tts>=6.1.9          # Text-to-Speech miễn phí, chất lượng cao
openai-whisper>=20231117  # Speech-to-Text offline

# Audio processing
pydub>=0.25.1
numpy>=1.24.0
scipy>=1.11.0

# Database & Memory (SQLite - không cần server)
aiosqlite>=0.19.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Async utilities
aiofiles>=23.2.0

# Optional - Cloud providers (nếu muốn dùng sau này)
# openai>=1.3.0
# anthropic>=0.7.0
# google-cloud-speech>=2.21.0
# google-cloud-texttospeech>=2.14.0
